/** Marca en pedidos al backend DK (web store API). */
export const STORE_BRAND = 'sf' as const

/** Categoría del menú seed / Prisma para pizzas Stone Fungus. */
export const STONE_FUNGUS_CATEGORY = 'Stone Fungus'

/** Clave de carrito en localStorage (alineada al web-store multi-marca). */
export const CART_STORAGE_KEY = 'dk-web-store-cart-sf'
